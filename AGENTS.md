# Work session reminder

When a work session changes files in this project, remind the user at the end to review the changes, commit them, and push the current branch to its remote repository.

Do not commit or push automatically unless the user explicitly asks.
